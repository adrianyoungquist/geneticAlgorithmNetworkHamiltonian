% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyperspheroid_sampling.R
\name{sample_hyperspheroid}
\alias{sample_hyperspheroid}
\title{Sample Points from a Hyperspheroid}
\usage{
sample_hyperspheroid(
  d,
  n,
  radius,
  center,
  scale_fac = 0.15,
  fix_edge = FALSE,
  fix_edge_value = 100
)
}
\arguments{
\item{d}{Number of dimensions.}

\item{n}{Number of points.}

\item{radius}{Sphere radius.}

\item{center}{Center point.}

\item{scale_fac}{Scaling factor applied to each dimension.}

\item{fix_edge}{Logical; fix first coordinate to a set value.}

\item{fix_edge_value}{Value to fix first coordinate to if \code{fix_edge = TRUE}.}
}
\value{
A matrix of sampled points.
}
\description{
Generates random points inside a distorted hypersphere to initialize parameter sets for exploration.

Samples random points from a distorted hypersphere, scaled based on the center's magnitude per dimension.
}
\details{
Sampling points from a hyperspheroid allows exploration around a center parameter set,
while preserving anisotropy based on the center's dimension scaling.
}
\examples{
set.seed(42)
points <- sample_hyperspheroid(3, 10, radius = 2, center = c(5, 10, 15))
plot(points[1, ], points[2, ], main = "Hyperspheroid Sample (First 2 dimensions)")
}
